export class CompanyModel {
  constructor(name, street, neighborhood, zipCode, city, state) {
    this.name = name
    this.street = street
    this.neighborhood = neighborhood
    this.zipCode = zipCode
    this.city = city
    this.state = state
  }
}
